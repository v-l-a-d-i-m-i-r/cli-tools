version: "2"
linters:
  enable:
    - errcheck
    - gocritic
    - govet
    - ineffassign
    - revive
    - staticcheck

formatters:
  enable:
    - goimports

issues:
  max-issues-per-linter: 50
  max-same-issues: 10

run:
  timeout: 5m
  tests: true
